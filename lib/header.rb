class Header
  attr_reader :string

  def initialize(string)
    @string = string
  end



  #take in information,



  #if there is # ...count it
  #set num = # count
  #once counted, return the num
  #use that num for the <h #{num}> opening and closing tags
  #{input} for the rest, spit it out

  def header_parser(string)



  end

  #def header_parser(input)
  #   if input.start_with?("#####")
  #     input.gsub("#####", "<h5>") + "</h5>"
  #   elsif input.start_with?("####")
  #     input.gsub("####", "<h4>") + "</h4>"
  #   elsif input.start_with?("###")
  #     input.gsub("###", "<h3>") + "</h3>"
  #   elsif input.start_with?("##")
  #     input.gsub("##", "<h2>") + "</h2>"
  #   else #input.start_with?("#")
  #     input.gsub("#", "<h1>") + "</h1>"
  #   end
  # end

  ##like the carpenter, other classes the metal guy, others ever the painter.
end
